<% case category %>
<% when "all" then displayed_dares = Dare.all %>
<% else displayed_dares = Dare.where(category: category) %>
<% end %>

<% if displayed_dares.count > 0 %>

<% array1 = []; array2 = []; displayed_dares.each_with_index { |dare,i| i.even? ? array1 << dare : array2 << dare } %>

  <div class="col-lg-6 order-lg-1 mt-0">
    <% array1.each do |dare| %>
      <div style="margin-top: 50px">
        <div class="container ">
          <div class="col-md-12 mx-auto eventCard">
            <%= render "shared/dare_card", dare: dare, friend: dare.creator, occasion: nil, disclaimer: false %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <div class="col-lg-6 order-lg-2 mt-0">
    <% array2.each do |dare| %>
      <div style="margin-top: 50px">
        <div class="container ">
          <div class="col-md-12 mx-auto eventCard">
            <%= render "shared/dare_card", dare: dare, friend: dare.creator, occasion: nil, disclaimer: false %>
          </div>
        </div>
      </div> 
    <% end %>
  </div>

<% else %>
<%# A AFFICHER SI VIDE %>
Aucun dare de cette catégorie ! Sois le premier à en créer un ! --> Bouton créer un dare (recopier le bouton + d'en bas à gauche)
<% end %>

<%= javascript_include_tag "user_cards_scripts" %>
