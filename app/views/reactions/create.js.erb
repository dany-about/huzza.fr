Array.from($(".reactionElement_<%= params[:reactionable_id] %>")).forEach( function(element) {$(element).html("<%= j (render 'shared/card_elements/reaction', event_id: params[:reactionable_id], type: params[:type]) %>")} )
