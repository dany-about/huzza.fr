<div class="container py-5 px-md-0 d-flex align-items-center">
  <div class="w-100">
    <div class="row row-grid justify-content-center justify-content-lg-between align-items-center">
      <div class="col-sm-8 col-lg-6 col-xl-5 order-lg-2">
        <div class="card shadow zindex-100 mb-0">
          <div class="card-body px-md-5 py-5">
            <div class="mb-5">
              <h6 class="h3">Connexion</h6>
              <p class="text-muted mb-0">Connecte toi poura accéder à ton compte.</p>
            </div>
            <span class="clearfix"></span>

            <!-- FORM -->
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class="field">
                <%= f.label :email, class: 'control-label' %><br />
                <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: 'Email...', class: 'form-control' %>
              </div>

              <div class="field">
                <%= f.label :"mot de passe", class: 'control-label' %><br />
                <%= f.password_field :password, autocomplete: "current-password", placeholder: 'Mot de passe...', class: 'form-control' %>
              </div>

              <% if devise_mapping.rememberable? %>
                <div class="field">
                  <%= f.check_box :remember_me %>
                  <%= f.label :se_souvenir_de_moi %>
                </div>
              <% end %>

              <div class="actions">
                <%= f.submit "Connexion", class: 'btn btn-soft-primary mb-3 mb-sm-0' %>
              </div>
            <% end %>

            <!-- FORM END -->

          </div>
          <div class="card-footer px-md-5"><small>Pas de compte ?</small>
          <%= link_to("Logout", destroy_user_session_path, :method => :delete)%><br>
          <span class="small d-sm-block d-md-inline">No account?</span><br>
          <%= link_to "Sign up", new_registration_path(resource_name), style:"small font-weight-bold"%><br>
          <span class="small d-sm-block d-md-inline">or</span><br>
          <%= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path, style:"small font-weight-bold"%>
      </div>
      <div class="col-lg-6 order-lg-1 d-none d-lg-block">
        <blockquote>
          <h3 class="h2 mb-4">La vie c’est comme tenter de mettre des chatons dans un panier. Quand on y met le dernier, le premier est déjà de l’autre côté. </h3>
          <p>— <cite class="text-lg">Joanna Trollope</cite></p>
        </blockquote>
      </div>
    </div>
  </div>
</div>
</div>
</div>