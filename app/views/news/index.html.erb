<!-- ICI COMMENCE LE FEED -->

<!-- bloc navigation -->
<div class="col-lg-3 order-lg-1 mt-3">
  <div data-toggle="sticky" data-sticky-offset="30" data-negative-margin=".card-profile-cover">
    <div class="card">
      <div class="list-group list-group-sm list-group-flush">
        <a href="account-profile.html" class="list-group-item list-group-item-action d-flex justify-content-between">
          <div>
            <i class="fas fa-user-ninja mr-2"></i>
            <span>Participations</span>
          </div>
          <div>
            <i class="fas fa-angle-right"></i>
          </div>
        </a>
        <a href="account-settings.html" class="list-group-item list-group-item-action d-flex justify-content-between">
          <div>
            <i class="fas fa-user-injured mr-2"></i>
            <span>Dares</span>
          </div>
          <div>
            <i class="fas fa-angle-right"></i>
          </div>
        </a>
        <a href="account-billing.html" class="list-group-item list-group-item-action d-flex justify-content-between">
          <div>
            <i class="fas fa-user-friends mr-2"></i>
            <span>Friends</span>
          </div>
          <div>
            <i class="fas fa-angle-right"></i>
          </div>
        </a>
        <a href="account-notifications.html" class="list-group-item list-group-item-action d-flex justify-content-between">
          <div>
            <i class="fas fa-sort-numeric-up mr-2"></i>
            <span>Account Metrics</span>
          </div>
          <div>
            <i class="fas fa-angle-right"></i>
          </div>
        </a>
      </div>
    </div>
  </div>
</div>
<!-- Section << Profil de mes amis >> -->

<% @friends_list.each do |friend| %>
<% end %>

<!-- Section << Achievements de mes amis >> -->
<div class="container">
  <table>
    <tbody>
      <% @network_news.each do |news| %>
        <!-- Si l'évènement est une participation; disclaimer_type peut prendre les valeurs: "participation_created" ou "participation_achieved" -->
        <!-- Il faudra voir comment on gère les participations qui sont "un-achieved", quand la preuve est rejetée -->
        <!-- status prend les valeurs true ou false selon que la notification a été lue -->

        <% if news.event.is_a?(Participation) %>
          <div style="margin-top: 80px">
            <%= render "shared/participation_card", participation: news.event, disclaimer: true, occasion: news.occasion, read: news.read %>
          </div>
        <% end %>
        <!-- Si l'évènement est un défi (i.e. défi créé, défi lancé ou défi relevé; disclaimer_type peut prendre les valeurs: "dare_created", "dare_sent" ou "dare_accepted") -->
        <% if news.event.is_a?(Dare) %>
          <div style="margin-top: 80px">
            <%= render "shared/dare_card", dare: news.event, disclaimer: true, occasion: news.occasion, read: news.read %>
          </div>
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>



<% if false %>
<div container>
  <div>
    <%= render 'shared/participation_card', participation: Participation.last, occasion: "participation_created", read: false %>
  </div>
</div>
<% end %>
